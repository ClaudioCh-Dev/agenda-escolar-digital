// Full AppModule e2e requires a running PostgreSQL instance (see .env.example).
// Auth flow is covered in auth.e2e-spec.ts with mocked services.

describe('AppModule (e2e)', () => {
  it('skipped — use auth.e2e-spec.ts; full module needs DATABASE_URL', () => {
    expect(true).toBe(true);
  });
});
